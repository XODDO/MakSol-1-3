

/*
class fans{
  public:
    fans(pin1, pin2){ //instead of pinMode(fanPin, OUTPUT)
        ledcAttachPin(fan, channel);
      //ledcAttach(Any_GPIO, channel[0-15])
  
        ledcSetup(channel, pwm_frequency, duty_cycle);
      //ledcSetup(channel[0-15], freq e.g 5,000, 1-16bit resolution)  
     
      }
    bool fan_1_ON = false; 
    unsigned long fan_1_duration = 0;
    unsigned long fan_1_stated = 0;
    
    if(fan_1_ON){
      if((millis() + fan_1_stated) >= fan_1_duration){
        
      }
    }


    bool fan_2_ON = false;
  
}
*/
